# Audrey Guinn — static Pages site (repo root holds index.html, assets/, social/).
# Deploy locally: wrangler login && wrangler pages deploy . --project-name=audrey-guinn-portfolio
# See `.github/workflows/cloudflare-pages.yml` for CI deploy.

name = "audrey-guinn-portfolio"
compatibility_date = "2026-05-03"
